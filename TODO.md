
## 🐛 Баги для исправления (найдено 08.08.2026)

### Import Duplicates (P0 - data quality)
- **Проблема**: Вариант 568718 имел 4 изменения цены за 22 мс ($10→$15→$10→$15) в `price_changes`
- **Причина**: Вероятно, дубли импорта при повторном запуске pipeline
- **Требуется**:
  1. Проверить идемпотентность импорта (idempotency check)
  2. Добавить UNIQUE constraint на `(variant_id, store_id, started_at)` в `price_changes`
  3. Добавить дедупликацию в pipeline перед записью
- **Приоритет**: P0 (влияет на всю историю и аналитику)
